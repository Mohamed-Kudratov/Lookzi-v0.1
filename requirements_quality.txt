# Lookzi Quality Pipeline — Faza 1 qo'shimcha dependencies
# O'rnatish: pip install -r requirements_quality.txt

# Garment background removal (rembg)
rembg>=0.0.7

# Super-resolution (ixtiyoriy, keyinroq)
# basicsr>=1.4.2
# realesrgan>=0.3.0

# Computer vision (sharpness + scoring uchun)
opencv-python>=4.5.0
